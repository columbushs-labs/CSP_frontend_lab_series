# 4.2 JS Arrays – Map, Reduce & Filter – Worksheet
## Pseudocode Practice

Write pseudocode (plain English steps) for each problem.

---

### 1. doubleAll(numbers)

Goal: Given an array of numbers, return a new array where each number is doubled using .map().



---

### 2. getNames(people)

Goal: Given an array of person objects with a `name` property, return an array of names using .map().



---

### 3. sumAll(numbers)

Goal: Given an array of numbers, return the sum using .reduce().



---

### 4. averageAge(people)

Goal: Given an array of person objects with an `age` property, use .reduce() to compute the average age.



---

### 5. filterAdults(people)

Goal: Given an array of person objects with an `age` property, use .filter() to keep only those age 18 or older.



