# 4.1 JS Functions Advanced – Worksheet
## Pseudocode Practice

Write pseudocode (plain English steps) for each problem.

---

### 1. callWithWelcome(message, callback)

Goal: Call the callback with the given message and then log that the callback has finished.



---

### 2. repeatThreeTimes(callback)

Goal: Call the given callback function three times.



---

### 3. delayedHello(callback)

Goal: Wait 1 second, then call the callback, which will print a greeting.



---

### 4. flexibleLog()

Goal: Using the `arguments` object, log each argument on its own line.



---

### 5. safeSum()

Goal: Add up all numeric arguments passed to the function and ignore any non-numeric arguments.



