# Vocabulary Worksheet – JS Arrays: sort() & reverse()

Define each term **in your own words** and add a short example where possible.

---

1. **Array**  
   - Definition:  
   - Example:

2. **In-place (mutation)**  
   - Definition:  
   - Example of a method that mutates an array:

3. **`.sort()`**  
   - Definition:  
   - What does it return?  
   - Does it mutate the original array?

4. **Compare function**  
   - Definition:  
   - What are the two parameters usually called?  
   - What should the function return to put `a` before `b`?

5. **Ascending order**  
   - Definition (for numbers and for strings):  
   - Example:

6. **Descending order**  
   - Definition (for numbers and for strings):  
   - Example:

7. **`.reverse()`**  
   - Definition:  
   - What does it do to the positions of the elements?

8. **Unicode / character code**  
   - What does it mean that strings are compared using their Unicode values?  
   - Why does this matter when sorting strings?

---

At the bottom of this page, write one **question** you still have about sorting or reversing arrays.
