# Guided Practice – Variable Scope, Hoisting & Shadowing

1. In your own words, what is **scope** in JavaScript?
2. List and briefly describe global, function, and block scope.
3. Why does a variable declared inside a function usually not exist outside of it?
4. How does `let` inside a for loop behave differently from `var`?
5. What is hoisting and how does it relate to `var`?
