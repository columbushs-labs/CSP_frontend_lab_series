# Guided Practice – Debugging, Error Handling & JSHint

Answer these before you start the coding lab.

---

1. List three tools or panels in DevTools that are helpful for debugging JavaScript.
2. What is the difference between a **syntax error** and a **runtime error**?
3. In your own words, what does a **stack trace** show you?
4. What does `try { ... } catch (error) { ... }` allow you to do in your code?
5. Why is it usually a bad idea to leave your users with an uncaught error on the screen?
