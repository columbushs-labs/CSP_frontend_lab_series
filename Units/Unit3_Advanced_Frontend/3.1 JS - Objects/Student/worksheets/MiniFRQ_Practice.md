# Mini FRQ Practice – JS Objects (Lab 3.1)

## Prompt
A `movie` object stores information about a film.

1) Create an object named `movie` with:
- title (string)
- year (number)
- rating (string)

2) Write code to:
- update the rating to `"PG-13"`
- add a new key `runtimeMinutes`
- delete the `year` key
- print all keys using `Object.keys(...)`

3) Reflection (1–2 sentences):
When would an object be better than using 3 separate variables?

Answer here:
______________________________________________________________
______________________________________________________________
