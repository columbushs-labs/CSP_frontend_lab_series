// src/calc.js
// Task: Implement a sum() and avg() function using TDD, mirroring the lesson.
// Start with sum(). The tests in calc.spec.js will guide you.
//
// TODO:
//   - Define a function sum(array) that passes all the specs:
//       * sum should be a function
//       * sum([]) should return 0
//       * sum() with no argument should throw Error('No parameter provided')
//       * sum([...]) should always return a number
//       * sum([...]) should return the correct total of the values
//
//   - Then, define a function avg(array) for the "Check for understanding":
//       * When given an array of numbers, return their average.
//       * Handle edge cases similar to sum() (e.g., empty array or missing parameter).

// Your code here:
