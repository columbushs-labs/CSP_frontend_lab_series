# Guided Practice – DOM Intro & Selectors

1. In your own words, what is the **DOM**?
2. What does `document` represent in JavaScript?
3. When would you use `getElementById` vs `getElementsByClassName`?
4. What is the main difference between `getElementsByClassName` and `querySelectorAll`?
5. Why do we often convert an HTMLCollection or NodeList into a real array before looping?
