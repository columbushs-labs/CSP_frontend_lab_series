# Guided Practice – DOM Manipulation

1. What is the difference between changing text with `innerHTML` vs using `createTextNode`?
2. How does `setAttribute` behave if the attribute already exists? What if it does not exist yet?
3. What does `removeAttribute` do if the attribute is not present on the element?
4. In your own words, what does `appendChild` do?
5. Why do we need to know who the parent node is when calling `removeChild`?
